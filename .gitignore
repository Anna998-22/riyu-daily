# ---------------------------------------------------------------------------
# 付费正文 —— 绝不进公开仓库
# ---------------------------------------------------------------------------
# 本目录放的是「买了才能看」的内容正文：主线 25 关、救命句付费段、以及
# 将来所有付费场景的对话/单词。GitHub Pages 是公开前端，这些文件一旦提交
# 就等于把付费内容送了。
#
# ⚠️ 重要：gitignore 只挡住「将来」的提交。本目录里的内容此前已经提交过，
#    仍留在 git 历史里（任何人 clone 后 git log 都能翻出来）。要真正从公开
#    仓库抹掉，还得重写历史或重建仓库 —— 光加这一行不够。
#
# ⚠️ 另有风险：这些文件从此**不在版本控制里**，没有备份。删了就没了。
#    改动前请自行留一份副本。
#
# 加载方式：js/app.js 的 loadPaidPack() 只在带 ?preview= 的会话里注入它们。
#    公开访客的浏览器不会主动请求本目录，所以线上既不会 404 也不会泄露。
js/paid/
